{
  "name": "plex-channels-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API + static-frontend server for plex-channels (queues.yaml/sets.yaml editing, MQTT bridge). Ships beside the Python plex-channels-queue service; frontend lives in ../web.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mqtt": "^5.15.2",
    "yaml": "^2.7.0"
  }
}
